# Commander Tier List

> [!CAUTION]
> Please note that this is _heavily_ vibe coded.

This is a simple app to rank various commander decks. It uses the Scryfall API to fetch the card images based on a central configuration file, and supports creating shareable URLs for your tier list.

## Uses

- Scryfall API
- Astro
- Tailwind CSS

## Helpful Commands

All commands are run from the root of the project, from a terminal:

| Command                | Action                                           |
| :--------------------- | :----------------------------------------------- |
| `yarn install`         | Installs dependencies                            |
| `yarn dev`             | Starts local dev server at `localhost:4321`      |
| `yarn build`           | Build your production site to `./dist/`          |
| `yarn preview`         | Preview your build locally, before deploying     |
| `yarn astro ...`       | Run CLI commands like `astro add`, `astro check` |
| `yarn astro -- --help` | Get help using the Astro CLI                     |
